angular.module(
    'eu.crismaproject.pilotE.controllers'
).controller(
    'exerciseDebriefingViewController', 
    [
        'DEBUG',
        function(DEBUG) {
            'use strict';

            if (DEBUG) {
                console.log('initialising exercise debriefing view controller');
            }
      
        }
    ]
);